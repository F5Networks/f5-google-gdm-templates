{
  "title": "{{ title }}",
  "data": [
    {
      "path": "{{ data.path }}",
      "properties": [
        {
          "name": "{{ data.name }}",
        "value": "{{ data.value }}"
      },
        {
          "name": "description",
          "value": "{{ page.description }}"
        },
        {
          "name": "sling:vanityPath",
          "value": "/connector-examples/json/"
        }
      ]
    }
  ]
}
